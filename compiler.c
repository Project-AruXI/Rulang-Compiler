#include "compiler.h"
#include "config.h"
#include "diagnostics.h"

extern Config config;

int compile(const char* filename) {
  rlog("Compiling Rulang file %s to assembly.", filename);

  return 1;
}